console.log("************** DELIVERABLE 01 *********************");
//1. Array operations
//Head
Implementa;
una;
función;
head(inmutable), tal;
que, dado;
un;
array;
como;
entrada;
extraiga;
y;
devuelva;
su;
primer;
elemento.Utiliza;
destructuring.
;
var head = function ( /* array */) { }; // Implementation here.
//Tail
//Implementa una función tail (inmutable), tal que, dado un array como entrada devuelta todos menos el primer elemento. Utiliza rest operator.
var tail = function ( /* array */) { }; // Implementation here.
//Init
//Implementa una función init (inmutable), tal que, dado un array como entrada devuelva todos los elementos menos el último. Utiliza los métodos que ofrece Array.prototype.
var init = function ( /* array */) { }; // Implementation here.
//Last
//Implementa una función last (inmutable), tal que, dado un array como entrada devuelva el último elemento.
var last = function ( /* array */) { }; // Implementation here.
//2. Concat
//Implementa una función concat (inmutable) tal que, dados 2 arrays como entrada, devuelva la concatenación de ambos. Utiliza rest / spread operators.
var concat = function (a, b) { }; // Implementation here.
//Opcional
//Implementa una versión del ejercicio anterior donde se acepten múltiples arrays de entrada (más de 2).
